
/*
// colortex0: main color buffer
const int colortex0Format = RGBA16F;
// colortex0 clears to fog color by default

// colortex1: normals (in player space)
const int colortex1Format = RGB8_SNORM;
const vec4 colortex1ClearColor = vec4(0.0, 0.0, 0.0, 1.0);

// colortex2: lighting information
const int colortex2Format = RG32UI;
const vec4 colortex1ClearColor = vec4(0.0, 0.0, 0.0, 1.0);
*/